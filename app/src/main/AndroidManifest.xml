<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        AnotherUserProfile

        <activity android:name=".AnotherUserProfile">
        </activity>

        <activity android:name=".SubscriptionsActivity">
        </activity>

        <activity android:name=".SignUpActivity">
        </activity>

        <activity android:name=".ProfileActivity">
        </activity>

        <activity android:name=".EditProfileActivity">
        </activity>

        <activity android:name=".IdeaProfileActivity">
        </activity>

        <activity android:name=".EditIdeaActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".AddIdeaActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".UserEditIdeaActivity"
            android:theme="@style/AppTheme">
        </activity>

    </application>

</manifest>